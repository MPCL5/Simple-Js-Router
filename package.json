{
  "name": "client-router",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "cy:run": "cypress open",
    "cypress:open": "start-server-and-test start http-get://localhost:3000 cy:run"
  },
  "devDependencies": {
    "cypress": "^9.5.0",
    "start-server-and-test": "^1.14.0",
    "vite": "^2.8.0"
  }
}
